#include "main.h"

/**
 * main - Entry point (Use the _printf() to print
 * a formated string)
 * Return: return 0 on success
 */
int main(void)
{
	_printf("Hello, %s! The answer is %c.\n", "User", 'A');

	return (0);
}
